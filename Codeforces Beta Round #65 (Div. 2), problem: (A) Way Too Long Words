#include <bits/stdc++.h>

using namespace std;

int main() {
 int n;
 string inp;
 int len;
 
 cin>>n;
 for(int i=0;i<n;++i){
 	cin>>inp;
 	len=inp.length();
 	if(len>10){
 	 
 	 cout<<inp[0] << len-2 << inp[len-1]<<endl;
	 }else {
	 	cout<<inp<<endl;
	 }
 }
 
 return 0;
 }
